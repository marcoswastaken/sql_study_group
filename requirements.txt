# Core dependencies for running the SQL Practice App
pandas>=1.5.0
datasets>=2.0.0
duckdb>=0.8.0
duckdb-engine>=0.9.0
flask>=2.0.0
flask-cors>=4.0.0

# Development tools (required for git hooks)
pre-commit>=3.0.0

# For development and additional features:
# pip install -e .[dev]
